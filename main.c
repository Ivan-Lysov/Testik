#include <stdio.h>
#include "Header_Practice_2_1.h"

void main(){
	/*One_Person** person;
	scanf_file("sad.csv", person);
	allocate_memory_for_one_person(person);
	print_person(person);
	sorting_for_surname(person, 3);
	print_person(person);
	free_memory_for_one_person(person);*/
	printf("10");
}